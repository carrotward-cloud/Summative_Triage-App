name: CI Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Run basic build step
      run: echo "Build step completed"

    - name: Run basic test step
      run: echo "Tests passed"

    - name: Deployment placeholder
      run: echo "Deployment step would run here"
